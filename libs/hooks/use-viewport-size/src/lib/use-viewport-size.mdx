import { Canvas, Meta } from '@storybook/blocks';

import * as UseViewportSizeHookStories from './use-viewport-size.stories';

<Meta of={UseViewportSizeHookStories} />

# useViewportSize

The `useViewportSize` hook provides the current width and height of the viewport. It updates the values whenever the window is resized.

<Canvas of={UseViewportSizeHookStories.Default} />